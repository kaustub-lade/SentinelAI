# Local dev (optional). If omitted, frontend uses /api and Vite proxy.
VITE_API_URL=http://localhost:8000

# Production on Vercel (optional if using vercel.json rewrites):
# VITE_API_URL=https://your-backend.onrender.com
